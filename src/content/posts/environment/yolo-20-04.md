---
# ==================== 必填字段 ====================

# 文章标题（必填）
title: Ubuntu 20.04 yolo 环境配置

# 发布日期（必填，格式 YYYY-MM-DD）
published: 2026-05-24

# ==================== 推荐填写的常用字段 ====================

updated: 2026-05-24
pinned: true
description: "yolo环境配置"
image: ./images/yolo.avif
tags: ["yolo"]
category: 环境配置

# ==================== 可选字段 ====================

lang: ""
author: ""
licenseName: ""
licenseUrl: ""
sourceLink: ""
draft: false
comment: true
slug: ""
password: ""
passwordHint: ""
---

## 环境配置

### miniconda安装脚本
```bash
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
```

### 禁用conda默认激活功能
```bash
source ~/.bashrc
conda config --set auto_activate_base false
```

### 创建一个独立环境（非base）
```bash
conda activate
conda create -n pytorch python==3.8
```

### 先进入刚创建好的环境中
```bash
conda activate pytorch
```

### 查看视觉画面pytorch本体安装（50系显卡下这个没问题）
```bash
pip install torch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 --index-url https://download.pytorch.org/whl/cu121
```

### 在虚拟环境中
```bash
conda activate pytorch
```

### 安装 ROS 桥接依赖 (非常关键！这让 Conda 里的 Python 能认出系统的 ROS 包)
```bash
pip install rospkg pyyaml
```

### 去github上之间下载ultralytics.zip 快
```bash
https://github.com/ultralytics/ultralytics
```

### 解压
```bash
unzip 
cd ~/ultralytics
pip install -e . -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 在虚拟环境中运行
```bash
mkdir -p ~/catkin_ws/src
cd ~/catkin_ws/src
catkin_create_pkg yolo rospy std_msgs sensor_msgs cv_bridge
```